<?xml version="1.0" encoding="utf-8"?>
<layout>
    <data>
        <import type="com.jereksel.libresubstratum.activities.themelist.IThemeListViewViewModel" />
        <variable
            name="viewModel"
            type="com.jereksel.libresubstratum.activities.themelist.IThemeListViewViewModel" />
    </data>

    <RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/activity_main"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context="com.jereksel.libresubstratum.activities.themelist.ThemeListView">

        <android.support.v4.widget.SwipeRefreshLayout
            android:id="@+id/swiperefresh"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:refreshing="@{viewModel.getSwipeToRefreshObservable()}"
            android:layout_below="@id/textView"
            app:onRefreshListener="@{() -> viewModel.reset()}">

            <android.support.v7.widget.RecyclerView
                android:id="@+id/recyclerView"
                app:layoutManager="android.support.v7.widget.LinearLayoutManager"
                app:items="@{viewModel.getAppsObservable()}"
                android:layout_width="match_parent"
                android:layout_height="match_parent" />

        </android.support.v4.widget.SwipeRefreshLayout>

    </RelativeLayout>
</layout>
